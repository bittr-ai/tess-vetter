name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"

      - name: Install locked dev dependencies
        run: uv sync --group dev --extra plotting

      - name: Lint
        run: uv run ruff check .

      - name: Type check
        run: uv run mypy src/tess_vetter

      - name: Tests (non-slow)
        run: uv run pytest -q -m "not slow"

  build:
    needs: quality-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.12"

      - name: Install locked dev dependencies
        run: uv sync --group dev

      - name: Build package
        run: uv run hatch build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: dist
          path: dist/

  test-build:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: dist
          path: dist/

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install from wheel
        run: |
          pip install dist/*.whl

      - name: Test import (wheel)
        run: |
          python -c "import tess_vetter.api as btv; print('Import successful')"
          python -c "from tess_vetter.api import Ephemeris, LightCurve, Candidate; print('Types imported')"

      - name: Install from sdist
        run: |
          pip uninstall -y tess-vetter || true
          pip install dist/*.tar.gz

      - name: Test import (sdist)
        run: |
          python -c "import tess_vetter.api as btv; print('Import successful')"
          python -c "from tess_vetter.api import Ephemeris, LightCurve, Candidate; print('Types imported')"

  publish-pypi:
    if: ${{ vars.PYPI_PUBLISH_ENABLED == 'true' && !contains(github.ref_name, 'rc') && !contains(github.ref_name, 'beta') && !contains(github.ref_name, 'alpha') && !contains(github.ref_name, 'dev') && !contains(github.ref_name, 'pre') }}
    needs: [build, test-build]
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/tess-vetter
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        # Uses trusted publishing (OIDC) - no token needed
        # Configure at: https://pypi.org/manage/project/tess-vetter/settings/publishing/

  github-release:
    needs: [build, test-build]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: dist
          path: dist/

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: true
          files: |
            dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-testpypi:
    if: ${{ vars.PYPI_PUBLISH_ENABLED == 'true' && (contains(github.ref_name, 'rc') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'alpha') || contains(github.ref_name, 'dev') || contains(github.ref_name, 'pre')) }}
    needs: [build, test-build]
    runs-on: ubuntu-latest
    environment:
      name: testpypi
      url: https://test.pypi.org/p/tess-vetter
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v7
        with:
          name: dist
          path: dist/

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
